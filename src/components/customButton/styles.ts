import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonContainer: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0,
      padding: 16,
      backgroundColor: '#363636',
      alignItems: 'center',
      borderColor: '#ededed',
      borderWidth: 1,
    },
    buttonText: {
      fontWeight: 'bold',
      fontSize: 18,
      color: 'white',
    },
  });