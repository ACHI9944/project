import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imageView: {
    borderWidth: 1,
    borderColor: '#ededed',
    padding: 8,
    borderRadius: 50,
  },
  image: {
    width: 22,
    height: 22,
  },
});
