import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    label: {
        marginVertical: 10,
        fontWeight: 'bold'
      },
      input: {
        height: 150,
        borderColor: '#ededed',
        borderWidth: 1,
        padding: 5,
        borderRadius: 10,
      }
})