import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    noteContainer: {
        backgroundColor: 'white',
        padding: 16,
        marginBottom: 8,
        borderRadius: 10,
      },
      noteHeader: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 4,
      },
      noteText: {
        fontSize: 14,
        color: '#333',
      },
})